pre-commit:
  parallel: true
  commands:
    frontend:
      glob: 'src/**/*.{ts,vue}'
      run: npx prettier --write {staged_files}
      stage_fixed: true
    backend:
      glob: '**/*.rs'
      run: cargo fmt --all
      stage_fixed: true
