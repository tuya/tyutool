# https://editorconfig.org — matches .gitattributes (LF for source)
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.{bat,ps1}]
end_of_line = crlf

[*.md]
trim_trailing_whitespace = false
